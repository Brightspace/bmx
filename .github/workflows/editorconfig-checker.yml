name: editorconfig-checker

on: [pull_request]

jobs:
  editorconfig-checker:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - uses: Brightspace/vulcan-shared-workflows/actions/editorconfig-checker@main
